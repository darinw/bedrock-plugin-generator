from tkinter import *

window = Tk()
window.title("Bedrock Plugin Generator")
window.geometry('350x200')

lbl = Label(window, text="Welcome to the generator")

lbl.grid(column=0, row=0)

window.mainloop()